---
title: "Asynchronous pending presentation — the weft#16 deferred follow-up"
ticket: Nodaste-Lab/weft#16 (successor stage; decision 9's deferral, now requested)
branch: claude/input-async-pending (cut from main at 5774e09, PR #19 merged)
status: in progress
predecessor: thoughts/plans/weft-input-design-system.markdoc (claude/input-design-system-16-impl)
---

# Asynchronous pending presentation

{% section id="condition" title="Why this opens now" %}

Decision 9 (2026-08-07) deferred asynchronous pending presentation out of weft#16
as unrequested scope, with the condition recorded as amendment A4 in
`docs/brand-package/12-input-heuristics.md`: *when asynchronous pending
presentation is introduced — commit starts evaluation; the consumer may prevent
progression while pending; Weft only presents the supplied pending or result
state.* Katie has requested the stage, and the consumer need is demonstrated:
Heddle's `SourceValidationStatus` is a live seven-state asynchronous vocabulary
that a pending/success/error model cannot express.

The A4 clause deliberately does not say Weft blocks navigation or submission —
it does not and will not. Decisions 7 and 10 stand: Weft never evaluates, and
the spy-based ownership guard in `commit-boundary.test.tsx` must keep passing
untouched.

{% /section %}

{% section id="groundtruth" title="Ground truth: the consumer's vocabulary, measured" %}

From `heddle/src/app/services/sourceValidationService.ts` and its consumers,
read 2026-08-13 (extending the four findings the handoff carried):

- **Seven statuses**: `validating | reachable | unreachable | degraded |
  blocked | unsupported-in-browser | unknown`.
- **The service itself never returns `validating` or `unknown`.** Pending is
  consumer-side: `SessionContextPanel` renders `Status: validating` when the
  result record is *absent* (line 685) — pending is the absence of an answer,
  not a status the check reports. `unknown` is likewise the consumer's
  not-yet-asked value. The remaining five are settled results.
- **The consumer already owns a tone mapping.** `severityForStatus` maps status
  → `ok | info | warning | danger`: reachable→ok; blocked, unreachable→danger;
  degraded, unsupported-in-browser→warning; validating, unknown→info.
  `degraded` is a *warning-toned result that is neither success nor failure* —
  `readable` stays true under it. This is the status-not-boolean finding as
  shipped code.
- **Results carry consumer text**: a `guidance` sentence and classified
  `warnings[]`. The words are the consumer's; no fixed vocabulary could hold
  them.
- **Weft precedent, distinct surface**: `ProviderStatusBadge` (in Weft since the
  Session Context work) presents a five-value *provider* vocabulary as a capsule
  badge. It is not field-attached and carries no describedby relationship; it is
  evidence that consumers keep inventing status presentation when the field
  doesn't offer one, not a base to extend.
- `SessionContextPanel`'s current rendering is the defect this stage exists to
  replace: a bare string, colour from severity as the only visual signal (the
  1.4.1 class the owner-validation round just purged from Weft), no
  describedby relationship, no reduced-motion consideration.

**Consequence for the contract**: Weft cannot own Heddle's seven statuses — the
next consumer will have seven different ones. What Weft standardises is the
*presentation contract*: a consumer-supplied pending flag or result tone plus
consumer-supplied text, rendered in the hint slot with the describedby, motion,
and exposure rules handled once. The consumer keeps the mapping from its own
vocabulary onto that contract, exactly as it keeps validation itself
(decision 7).

{% /section %}

{% section id="contract" title="The presentation contract" %}

**Both layers, parity, no new allowlist entry** — this is CSS + markup
convention in the plain layer and a thin component in React; no JavaScript
behaviour is required for the plain layer to reach the same rendering.

- **Placement: the hint slot.** The field's right edge is taken (chevron,
  search clear, error glyph — asserted by `error-icon.spec.ts`); the pending
  affordance adds nothing there. Plain CSS: `.weft-field-hint.is-pending`.
  React: a new `FormStatus` member of the `Form` family rendering into the same
  slot. `Input` gains no prop (counter-guard: props snapshot untouched for
  `input`).
- **Pending is text with a pulsing dot**, reusing `weft-pulse`, whose final
  keyframe is opacity 1 — under Weft's reduced-motion freeze (0.001ms, one
  iteration) the dot measures static-visible rather than reading as a hung
  field. The dot is also the non-colour shape signal for pending.
- **Results are a tone axis, not a boolean**: `ok | info | warn | stop`,
  landing on the existing tokens `--weft-ok / --weft-info / --weft-warn /
  --weft-stop` (the same four axes as Heddle's `severityForStatus`; `warn`
  carries `degraded`). The consumer's text carries the meaning; tone colour
  reinforces and is never the only signal distinguishing two states the
  consumer renders with identical text — specimens must not ship identical-text
  tone pairs. A `stop`-toned status is presentation only: whether the field is
  *invalid* (`aria-invalid`, error machinery, A5 message) stays the consumer's
  call, per decision 7.
- **Describedby: A5's order extended, not reordered.** One ordered reference
  list: error id first, status id second, help id third. Error keeps first
  position (urgent thing first — A5's rationale); status precedes durable help
  because it is the newest fact about the field. Existing pairs keep their
  relative order, so every shipped A5 case is unchanged. Plain-CSS convention:
  status ids end `-status`, enforced by the specimen structural gates alongside
  the existing `-hint`/`-error` rules.
- **`aria-busy` on the control while pending**, per the P2-era measurement
  (`busy: 1` with the description resolving). React: `FormStatus`'s pending
  registration reaches the control through the existing item context. Plain
  CSS: a markup convention — a control whose describedby references an
  `is-pending` hint carries `aria-busy="true"` — enforced page-wide by the
  specimen gate in both directions (pending hint without busy fails; busy
  without a pending hint fails).
- **Replacement, not stacking.** A status change replaces the text under the
  same id; ids never duplicate in the reference list. Staleness and
  cancellation are consumer-owned — the consumer decides *what* state to
  supply and when; Weft's whole behaviour is to present the current supplied
  state.
- **Exposure, never announcement.** Every claim in tests and prose: the
  description *resolves* in the accessibility tree. A change to a describedby
  target is not a dependable live update, and nothing here claims otherwise.

**Non-goals**: evaluation of validity (decisions 7/10); blocking navigation,
submission, or progression (A4's own clause); announcement claims; QuietField /
DisclosureField (decision 8 — still waiting on two independent consumers);
Heddle's own migration of `SessionContextPanel` (its repo, after release).

{% /section %}

{% section id="tests" title="Test manifest (written before implementation)" %}

| File | Claims |
|---|---|
| `src/ui/__tests__/form-status.test.tsx` | FormStatus renders supplied text in the hint slot; pending dot present; tone attribute for each of the four tones; describedby order error→status→help asserted by POSITION across the A5 case matrix extended with status (help+status; error+status; all three; error removed, status+help survive; status removed, ids leave the list; no duplicated id); replacement under a stable id; `aria-busy` on the control while pending and gone when not; ownership spies — supplying pending/status calls no consumer callback, writes no value, touches no react-hook-form state. |
| `src/ui/__tests__/source-validation-vocabulary.test.tsx` | The consumer-shaped transition suite against Heddle's seven states: a mapping fixture (status → {pending | tone, text}) drives FormStatus through validating→reachable, validating→degraded (warning result, field NOT invalid, readable), validating→unreachable (consumer chooses error machinery; status yields to A5 message order), reachable→validating (revalidation), unknown and unsupported-in-browser; each transition asserts the exposed describedby state and that Weft performed no evaluation (spies). |
| `tests/contract/input-pending.spec.ts` | Rendered: the pending dot measures opacity 1 (static-visible) under `prefers-reduced-motion: reduce` and animates otherwise; the field's right edge is unchanged by pending (no new painted element in the reserve; padding untouched); tone colours resolve from the four tokens per palette × theme; specimen describedby order holds error→status→hint. |
| `scripts/__tests__/input-specimens.node.mjs` (extended) | Structural gates: `-status` id convention resolves; order error→status→hint page-wide; `aria-busy` ⇄ `is-pending` pairing both directions; no identical-text tone-pair specimen. |
| Counter-guards | `input` props snapshot untouched; `commit-boundary.test.tsx` ownership spies untouched and passing; `css/weft.css` diff additive-only (`check-pure-token-file`); no new parity gap (both layers ship). |

Every guard added here gets probed by reintroducing its violation before it is
believed. A guard that has not failed has not been verified.

{% /section %}

{% section id="phases" title="Phases" %}

- **PA — the presentation** (this document's contract, tests first, both
  layers, specimens, doctrine hooks: A4 flipped from condition to in-force,
  A5's order note, both as amendments in `12-input-heuristics.md` with the
  heuristics' amendment conventions; flagged to Katie in the phase report —
  doctrine wording is hers to overrule).
- **PB (secondary, only if PA blocks or closes with scope to spare)** — the
  gallery error card (the visual suite photographs no invalid specimen), and
  the dependency-free commit-boundary helper closing both allowlisted parity
  gaps (`commit-boundary`, `search-clear-action`, expiry 2027-02-28).

Close PA double-clean via `scripts/review-gate.sh` and report on a new PR.
Codex is usage-capped until 2026-08-17; the live second reviewer is Katie's
Cursor tracking board — ask her to run it rather than self-waiving.

{% /section %}

{% section id="log" title="Decisions / Deviations Log" %}

- 2026-08-13: Stage opened on Katie's request; deferral condition met by
  Heddle's seven-state `SourceValidationStatus`. Read first, in order: the
  handoff (`thoughts/handoffs/weft-16/2026-08-12_input-async-pending-followup.md`,
  branch `claude/input-design-system-16-impl` @ fa493c7), the predecessor
  plan's Decisions/Deviations Log, PR #19's thread end to end (50 comments,
  including all 21 tracking boards).
- 2026-08-13: Sequencing gate checked: PR #19 MERGED (5774e09, 2026-08-13).
  **0.4.0 not yet released** — the Release workflow versioned to 0.4.0 and
  pushed `changeset-release/main` (a5e3cb7) but the org blocks Actions from
  opening PRs, the same failure #18 resolved manually. Version Packages PR
  **#20** opened manually to unblock; merging it publishes 0.4.0 and is
  Katie's. Recorded as a deviation from the gate's letter (merged ✓, released
  ✗): the branch is cut from merged main, which the release does not change;
  nothing in this stage depends on the artifact. Flagged rather than waited
  out — Katie can overrule by pausing this branch until #20 merges.
- 2026-08-13: Branch `claude/input-async-pending` cut fresh from `main` at
  `5774e09` with its own `node_modules` (`npm ci`). Working in the
  `zen-herschel` worktree — the one the external Cursor agent reset three
  times mid-P7 — so every unit of work commits and pushes immediately.
- 2026-08-13: The vocabulary mapped from the consumer's shipped code rather
  than from design (the decision-9 instruction). Two findings beyond the
  handoff's four: the service never returns `validating`/`unknown` — pending
  is the consumer-side absence of an answer, which is why Weft's contract
  takes a supplied pending flag rather than reserving a status string; and the
  consumer already ships the four-axis tone mapping (`severityForStatus`),
  which is what the tone axis lands on. `ProviderStatusBadge` noted as
  precedent-not-base.
- 2026-08-13: `aria-busy` settled on the control (not the status element), on
  the P2-era measured precedent (`busy: 1`, exposure verified 2026-08-07).
  Presentation of a supplied state, inside decision 7's line.
- 2026-08-13: Describedby order settled as error → status → help: A5's
  rationale (urgent first) keeps error first; status is newer than durable
  help; no shipped A5 case changes relative order. Recorded as a proposed
  amendment for the heuristics file; wording is Katie's to overrule at the
  phase report.
- 2026-08-13: **PA built, tests first throughout.** The two vitest suites
  (25 assertions) written failing against a missing export; FormStatus +
  the FormItem status registry landed them 34/34 with the shipped A5 suite
  untouched. Plain CSS: `.is-pending` dot + four tone classes inside the
  hint-slot block, declared BEFORE `.is-error` so a doubled-up element stays
  an error on the cascade tie; `css/weft.css` untouched — no new token was
  needed. Seven specimens; gates S8/S8a/S8b extended (pairwise
  error<status<hint), S14 (busy ⇄ pending, both directions) and S15
  (identical-text tone pairs refused) added; `input-pending.spec.ts` holds
  the freeze contract, the untouched right edge, tone-token resolution and
  CDP exposure (busy: 1). Form 1.1.0 → 1.2.0; `input`'s snapshot untouched.
- 2026-08-13: Probe ledger: 27 probes run, 27 CAUGHT — but two only on the
  second attempt, and both first attempts are the standing lesson again. The
  S14 probe stripped `aria-busy` from before `aria-describedby` when the
  generator emits it after (mutated nothing, gate "passed"); the fade-out
  probe replaced the FIRST `animation: weft-pulse …` line in the file, which
  is `.weft-pill::before`, not the dot (guard "passed" an unmutated dot).
  Both rebuilt against the real rule and then CAUGHT. A probe that mutates
  the wrong thing is indistinguishable from a decorative guard.
- 2026-08-13: Doctrine: A4 flipped to in force with the condition and its
  meeting recorded; A9 added (status joins the ordered list); 04's Form
  inputs section gained the Asynchronous status subsection, the extended id
  convention and a supplied-status ownership row. D1 taught that a backticked
  weft- name may ship as `@keyframes` — and probed with a fake one, CAUGHT.
  Found in passing: `test:doctrine` ran in CI and release but was missing
  from review-gate.sh's candidate battery despite the P8 log claiming it
  joined; added, and the review-gate self-suite stays 62/62.
- 2026-08-13: Typing probe pins the tone axis closed under `--strict`
  (probed by widening — CAUGHT); the packed root-entry shape probe carries
  FormStatus (probed by dropping the barrel line — CAUGHT).
- 2026-08-13: The Codex cap had reset early — probed with a one-word prompt
  before assuming the handoff's Aug-17 date, and the real double-clean gate
  ran instead of waiting on a substitute reviewer.
- 2026-08-13/14: **PA closed gates PASS · codex CLEAN at `03b3e67`** (PR #21
  marked ready). Five gate rounds, five findings, one pair of
  nits — every fix at the class, every guard probed: (r1) the pending cells
  were a Python TUPLE interpolated into the grid — visible repr punctuation
  on the committed page that every selector-based gate read past; S16 now
  refuses stray direct text in any .grid, probed with the original defect.
  (r1) FormStatus registered on a passive effect — a pending field could
  paint a frame unreferenced and not busy; registration moved to a layout
  effect, held by flushSync tests that fail on useEffect, with the SSR
  hydration boundary stated and pinned. (r2→r3) nits batched: changeset
  named a class that doesn't exist; checklist line points at the status
  slot. (r3) the info status was a new first-class text use of light
  `--weft-info` at 2.54:1 — fixed as `--weft-info-text` (AA, per-palette
  incl. hud-glass with a coverage test, ON_PAPER measures it) WITHOUT
  taking the open `--weft-info` lift decision, which stays Katie's. (r4)
  the replacement-not-stacking contract had no enforcement — the registry
  now keys on the instance and a second live FormStatus throws, with
  present/pending derived from the entry rather than effect order.
- 2026-08-13/14: **Probe-restore rule, learned twice in one session and now
  binding**: probes restore from scratch-directory snapshots, NEVER from
  git, until the unit is committed — `git checkout` reverted uncommitted
  work under two separate probes (the weft.css token declarations; the
  round-4 registry), and in the first case the gate then PASSED against a
  token that did not exist because ON_PAPER skips what does not resolve.
  That silent-skip hole is itself closed (the coverage test requires the
  base declaration; probed by deleting it — CAUGHT).
- 2026-08-14: **The close is NOT double-clean, by owner correction.** Katie's
  rule, now on the record: "double-clean" is reserved for a verdict that
  includes an ANTAGONISTIC tester — gates plus a Codex CLEAN is a cooperative
  review, and the P8 evidence is exactly why the distinction matters (the
  Cursor board's adversarial probe caught the `form.reset()` hole six Codex
  rounds missed). review-gate.sh's own "DOUBLE-CLEAN" banner uses the looser
  sense; the script's label is not the claim. PR #21's earlier close comment
  is corrected in-thread. PA's honest state: gates PASS · codex CLEAN ·
  antagonistic pass NOT yet run — the phase does not close until the board
  (or an equivalent adversarial prober) has had it.
- 2026-08-14: **The board ran, twice.** First pass at `420fc2f`: NOT CLEAN —
  one hard finding (status-without-help dangled the description id; the
  rewrite made only the status arm element-conditional and the fixture
  matrix never composed the case) plus two softs (Slot consumer-override;
  the React dot's freeze unmeasured). Fixed at the class at `785378a`:
  element-tracked inclusion for EVERY describedby arm (description and
  message register presence; message only while carrying content — which
  also closed the unnamed arm, an error presented outside FormMessage); a
  bare control carries no aria-describedby; the SSR boundary widened
  symmetrically and re-pinned; both softs taken as stated-and-pinned
  boundaries. Three probes, three CAUGHT (5/1/10 failures). Second pass:
  **board CLEAN at `785378a`** — its probes re-run PASS, the softs accepted,
  the FormDescription/FormMessage singleton left explicitly as an owner
  call.
- 2026-08-14: **v0.4.0 released** — Katie merged PR #20 and the Release
  workflow published and tagged from main on its own (it only ever failed at
  PR creation). Heddle's adoption steps are unblocked in its repo.
- 2026-08-14: The codex re-bind after the board's CLEAN caught one more
  blocker before going CLEAN at `ff67f4c`: `design-system.html` — the
  shipped self-contained rendered reference — still taught the pre-async
  form-input section. The page now embeds the status CSS (weft-pulse
  keyframes; info on `--weft-info-text`), renders pending + degraded
  specimens with the aria-busy pairing and a code sample, and S11 grew
  probed teeth (no-pending-specimen / busy-dropped / info-regressed all
  fail — 3 probes CAUGHT). Nits: the README's amendment count is REMOVED
  rather than corrected (a number in prose is a number that drifts), and
  the rendered tone checks sweep palette × theme. En route, an aborted gate
  run exposed machine-level environment damage: 24 duplicated " 2" folders
  in `node_modules/@types` (tracked tree clean; fresh install; the same
  pattern exists in the Heddle checkout — the duplicator is not this repo's
  process and is worth hunting like the worktree resetter was).
- 2026-08-14: Where the claim stands, precisely: gates PASS · codex CLEAN
  at `ff67f4c`; **board CLEAN at `785378a`**, with the delta since being
  reference-page/README/test-coverage only — whether that delta needs a
  board re-check is the board's call, because the verdict binds to the SHA.
  Remaining for Katie: doctrine wording (A4/A9/aria-busy) to overrule or
  bless; the open `--weft-info` lift decision; the
  FormDescription/FormMessage singleton call; the board's word on the
  docs-only delta; merge. PB (gallery error card; the vanilla
  commit-boundary helper for both 2027-02-28 parity gaps) stays available
  to a future session — not started here.
- 2026-08-14: **The `--weft-info` decision settled by Katie: keep the
  split.** `--weft-info` stays #60a5fa for non-text uses — and, found while
  closing it, for one legitimate dark-canvas TEXT use: the board's
  provenance badge (`.weft-badge.is-info`), where the light blue measures
  6.8:1+ on the dark slab and the dark text-blue would have been the
  hud-glass trap inverted. Info-as-text on paper/cream rides
  `--weft-info-text` #2563eb. Mechanics of the close: `--weft-info` left
  the contrast gate's ON_PAPER list (it is not an on-paper text token by
  owner decision) and both its KNOWN_FAILURES entries came out —
  `--weft-danger`'s entries remain, that decision still open. The split
  gained its counter-guard: no `.weft-field-hint` rule may paint with the
  bare token, probed by reintroduction and — the probe ledger's recurring
  lesson — the guard's FIRST version failed on a ReferenceError the probe
  read as CAUGHT (`componentsCss` was never loaded in that file); rebuilt,
  and the re-probe fails with the guard's own message. Decision records
  updated in weft.css, weft-components.css, 04-design-system, and the
  changeset.
- 2026-08-14: **Owner approvals, recorded together (Katie, in-session):**
  the doctrine wording stands as written (A4 in force, A9's
  error → status → help order, aria-busy on the control); the
  FormDescription/FormMessage singleton stays PRESENCE-ONLY — the throw is
  FormStatus's alone, and retrofitting it onto 1.x components is declined;
  and the merge of PR #21 is approved, to execute after the board's
  re-probe of the heritage glyph fix, green CI, and a final gate re-bind
  at the merge tip. The release (Version Packages, 0.5.0) stays the
  owner's merge, per the #18/#20 precedent.
- 2026-08-14: Board pass 5 (the heritage glyph) fixed and probed; en
  route, two self-catches for the ledger: a probe read that looked like a
  MISS because tail -1 swallowed the failure line (the guard was fine, the
  capture was not), and a CI round lost to a bare hex in my own glyph
  COMMENT — check-raw-colors does not exempt prose, and my partial local
  battery had skipped that gate. Full battery before push, not after.
- 2026-08-17: **PB executed** (branch `claude/gallery-status-and-error-cards`,
  PR #23, cut from 0.5.0 main) — the gallery half: the `form-status` card
  (pending with the dot and aria-busy; the four tones on distinct words;
  the error + status + help composition holding A9's order) and the
  `input-error-states` card (composed field with the glyph-led message;
  a standalone invalid input — see the finding below for why standalone;
  invalid textarea; the select exception; dashed disabled + invalid).
  Tests first (10 assertions failing, then the cards); nine probes, nine
  CAUGHT, restores from scratch snapshots. Baselines: 8 added, 40 moved,
  every one reviewed individually in the pinned container — the moves are
  two mechanical classes of inserting cards into the ordered auto-fit
  grid (row-mate stretch; ±1px column rounding), old/new pairs eyeballed.
  Showcase-only ids carry their shelf category in a gallery-local map
  because manifest.uiPrimitives must mirror `src/ui/*.tsx` exactly; the
  map is test-pinned. The vanilla commit-boundary helper (PB's other
  half) is NOT started here. Two findings flagged for their own sessions:
  an input composed under FormControl loses `data-slot="input"` (the
  Slot's props land after Input's own attribute), so the composed path
  never matches the CSS trailing-glyph selector — the two-layers-cannot-
  drift claim has a hole the contract suites never photograph; and the
  `search-field` card is absent from SHOWCASED_PRIMITIVE_IDS, so it never
  renders on the site despite manifest `showcase: true`.
- 2026-08-17: Codex round 1 (at 9023a8c): ISSUES — the auto-fit grid's
  default stretch alignment let the two tall new cards inflate every
  row-mate's height, so 40 unrelated baselines moved and every FUTURE
  card insertion would rewrite dozens more. Fixed at the class:
  `alignItems: 'start'` on the gallery grid — card height is now
  content-determined and baselines insertion-order-independent (the ±1px
  three-column rounding class remains, order-dependent by nature).
  Baselines re-recorded in the container and re-reviewed individually.
- 2026-08-17: Codex round 2 CLEAN at 9b2a887 with a comment nit, batched
  into the next round. Round 3: ISSUES — the select error specimen showed
  border alone where the doctrine's exception is border PLUS the glyph-led
  message; fixed with a hand-associated `.weft-field-hint.is-error`
  (not composed under FormControl — the Slot would swallow the trigger's
  slot identity, the same class as the flagged Input finding), test
  extended to require the referenced message, two probes CAUGHT.
- 2026-08-17: **A correction for the ledger**: an earlier commit claimed a
  passing verify run proved baselines "byte-stable" — false; a verify PASS
  proves comparator-stable only. Update runs kept rewriting ~26 files and
  consecutive verifies failed on DIFFERENT subsets (select, then
  stat-row + switch, then panel-header + table): the gallery's fractional
  column geometry (1032px grid → 334.66px columns) made card rasterization
  flap between rounding states run to run. Fixed at the class: the site
  wrapper is 1078px so the grid divides to 334px integer columns at
  integer x. Evidence: three consecutive container verifies PASS and two
  consecutive UPDATE runs produce zero byte differences — that is what
  byte-stable actually looks like. All 84 baselines re-recorded once at
  the integer geometry (every delta −1/−2px width, ±1px wrap rounding;
  callout-dense +16 is one caption wrapping; input-error-states +27 is
  the select message), each quantified, outliers eyeballed.

{% /section %}
